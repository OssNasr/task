import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  mark: {
    width: 10,
    height: 10,
    marginHorizontal: 6,
  },
  markText: {
    color: 'gray',
    fontSize: 12,
  }
})

export default styles